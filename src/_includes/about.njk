{% extends "./default.njk" %}

{% block main %}
<main class="{{ bgColor }} text-grid about">
  <h2>{{ pageTitle }}</h2>
  <div class="text-content">
    {{ content | safe }}
  </div>
</main>
{% endblock %}
